use helpers::{
    lines_from_file,
    DATA_FILE_PATH,
};
use std::{
    path::PathBuf,
};


fn main() {
    let input_file = PathBuf::from(DATA_FILE_PATH).join("day03_input.txt");
    println!("Hello, world!");
}
